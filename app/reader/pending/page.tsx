// Reader-side pending dashboard. Reuses the same applicant component as the
// academy pending page so that the experience (form, status timeline, reapply)
// is identical for teachers and readers.
export { default } from "@/app/academy/pending/page"
